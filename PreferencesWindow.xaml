<Window x:Class="DevBar.PreferencesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DevBar Preferences"
        Width="450" Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FF2D2D30">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="URL:" Foreground="#CCCCCC" VerticalAlignment="Center" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="UrlBox" Margin="0,0,0,8" />

        <TextBlock Grid.Row="1" Text="Refresh (seconds):" Foreground="#CCCCCC" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="RefreshBox" Margin="0,0,0,8" Width="60" HorizontalAlignment="Left" />

        <CheckBox Grid.Row="2" Grid.ColumnSpan="2" x:Name="StartupBox"
                  Content="Start with Windows" Foreground="#CCCCCC" Margin="0,4,0,0" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="0,0,8,0" Click="OnSave" />
            <Button Content="Cancel" Width="80" Click="OnCancel" />
        </StackPanel>
    </Grid>
</Window>
